# Tailscale Synology DSM r3 evidence index

This privacy-reviewed record binds the accepted r3 source, layered patch series, two byte-identical build pairs, static inspection identity, package hashes, and attended DSM 7.4.1 acceptance. Raw logs and package binaries remain outside Git.

The r2 58-patch base and r3 seven-patch delta are both verified in order. Destructive lifecycle coverage is deliberately not inferred.
