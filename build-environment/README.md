# Build environment

The Tailscale SPK and the DSM netfilter-module package use separate build
environments. Definitions in this directory will pin the tools and inputs
used for candidate and production builds.
