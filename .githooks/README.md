# Git hooks

Version-controlled hooks will enforce fast local checks. Remote validation
remains authoritative because local hooks can be bypassed.

Planned hooks:

- `pre-commit`
- `commit-msg`
- `pre-applypatch`
- `pre-push`
- `post-rewrite`
