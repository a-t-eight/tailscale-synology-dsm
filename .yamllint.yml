extends: default

ignore: |
  .git/
  build/
  dist/
  out/
  node_modules/

rules:
  document-start: disable
  line-length: disable
  truthy:
    allowed-values:
      - "true"
      - "false"
      - "on"
      - "off"
    check-keys: false
